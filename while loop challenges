WHILE LOOP CHALLENGE 1

total = 0
while total <= 50:
    number = int(input("Enter a number >>>"))
    total = total + number
    print("The total is", total)
    if total > 50:
        print(total, "is over 50")
